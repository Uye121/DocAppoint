global:
  scrape_interval: 30s
  evaluation_interval: 30s

scrape_configs:
  - job_name: 'django-backend'
    static_configs:
      - targets: ['host.docker.internal:8000']
    metrics_path: '/metrics'